```mermaid
graph TD
    subgraph "Client (Web Browser)"
        UI[Main UI / Index.html]
        JS[main.js]
        V_Feed[Video Feed Display]
    end

    subgraph "Flask Server (Main)"
        APP[app.py]
        VS[vision.py]
        SIO[Socket.IO]
    end

    subgraph "Hardware Control"
        RM[robot_manager.py]
        R0[Robot 0: Leader]
        R1[Robot 1: Follower]
    end

    %% 데이터 흐름
    UI -->|User Control| JS
    JS -->|Socket Emit| SIO
    SIO --> APP
    APP --> VS
    VS -->|Process Frame| APP
    APP -->|Command| RM
    RM -->|TCP/IP Socket| R0
    RM -->|TCP/IP Socket| R1
    R1 -->|Encoder Data| RM
    RM -->|Socket Emit| SIO
    SIO --> JS
    JS -->|Update UI| UI